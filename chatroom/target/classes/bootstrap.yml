server:
  port: 8050

spring:
  application:
    name: room-server
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        service-id: config-server
        enabled: true
  redis:
    password: 94450
    port: 6379
    host: 47.94.87.74
    timeout: 6s
    jedis:
      pool:
        max-active: 200
        max-idle: 100
        max-wait: 1s
        min-idle: 50
  rabbitmq:
    host: 47.94.87.74
    virtual-host: /
    username: guest
    password: guest
eureka:
  client:
    service-url:
      defaultZone: http://eureka1:8761/eureka,http://eureka2:8762/eureka
  instance:
    prefer-ip-address: true

user:
  service:
    name: user-server:8081